org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.newDsl=false
android.builtInKotlin=false
# Workaround: Kotlin incremental compilation breaks on Windows when project (D:)
# and pub cache (C:) are on different drive letters. Kotlin 2.x cannot compute
# relative paths across drive roots, corrupting the cache.
kotlin.incremental=false
